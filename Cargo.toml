[package]
name = "s3"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1"
surrealism = "0.5.0"
surrealdb-types = "3.2.4"
# surrealdb = { version = "3.2.4", default-features = false }  # optional; only if you need the full SDK crate
